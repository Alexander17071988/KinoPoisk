import React from "react";
import MovieCarousel from '@widgets/MovieCarousel/ui/MovieCarousel';

const Home: React.FC = () => {
    return <>
        <MovieCarousel />
    </>
};

export default Home;